{
  "name": "plaud-recording-downloader",
  "version": "0.1.0",
  "description": "Chrome extension that helps download Plaud recordings.",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && mkdir -p dist && cd extension && zip -qr ../dist/plaud-recording-downloader.zip .",
    "dev": "echo 'Load the unpacked extension from the extension/ directory in Chrome.'"
  }
}
