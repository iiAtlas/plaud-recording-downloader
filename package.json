{
  "name": "plaud-recording-downloader",
  "version": "1.0.0",
  "description": "Chrome extension that scans Plaud recordings and downloads the audio in bulk.",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && mkdir -p dist && cd extension && COPYFILE_DISABLE=1 zip -qr ../dist/plaud-recording-downloader.zip .",
    "dev": "echo 'Load the unpacked extension from the extension/ directory in Chrome.'"
  }
}
